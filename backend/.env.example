NODE_ENV=development
PORT=4000

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=procheeck

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Public URL for building links in emails
APP_URL=http://localhost:3000

# Resend (optional — if unset, dev mode logs email content to backend console)
RESEND_API_KEY=
MAIL_FROM=PROCHEECK <onboarding@resend.dev>
